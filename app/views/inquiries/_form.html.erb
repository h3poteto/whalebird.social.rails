<%= simple_form_for(@inquiry, wrapper: :horizontal_form, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, placeholder: "hogehoge@hoge.com" %>
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.submit '送信する', class: "btn btn-primary" %>
  </div>
<% end %>
